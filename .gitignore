# -----------------------------
# Secrets & environment
# -----------------------------
/.ssh
/.composer              # anchor: only root-level .composer
/.composer/auth.json
.env
.env*                   # any .env files anywhere

/.htaccess              # anchor: root .htaccess only
/config/composer.auth.json
/auth.json              # consider anchoring to root if you only keep one: /auth.json

# -----------------------------
# Dependencies / package managers
# -----------------------------
/vendor
/node_modules
# Composer installers & phars (optional to anchor)
/composer.phar
/composer-setup.php

# Lockfiles (decide policy)
# - Keep ignored for flexible installs, OR
# - REMOVE this line to commit composer.lock for reproducible builds
composer.lock

# Node lockfile is commonly ignored
package-lock.json

# -----------------------------
# Build / generated artefacts
# -----------------------------
/dist
# If these are build outputs (not source), keep ignored:
/public/assets/js
/public/assets/reels
/public/assets/project

# -----------------------------
# App data / runtime caches
# -----------------------------
/data
/var
!/var/.gitkeep
!/var/notes-sample.json
!/var/notes.json
!/var/source_code.json
/var/packages/*
!/var/packages/psr-log.php
/var/weekly-timesheet-????-??.json
!/var/weekly-timesheet-sample.json

# -----------------------------
# Logs & PIDs
# -----------------------------
/var/log/php_error.log
/server.log
/server.pid

# -----------------------------
# Project scaffolding / examples
# -----------------------------
/public/assets
/public/*.map
/public/example.php
/public/example*.php

# OS/editor
.DS_Store
Thumbs.db
settings.json

# -----------------------------
# Big trees you don�t want in Git
# -----------------------------
/applications
/bin
/clients/*
# Project slots like project0/, project1/
# Keep only their index.php tracked
/project?/*
!/project?/index.php

# -----------------------------
# Tests (opt-in)
# If you want tests versioned, remove the next line
# -----------------------------
/tests

# -----------------------------
# Misc
# -----------------------------
/tasks.txt
/main.js               # anchor if you mean only the root one
notes*.txt
*.old
settings.json          # ignore if personal/runtime; remove if it should be versioned
*.config.js